spring:
  application:
    name: Cantina
  datasource:
    url: jdbc:mysql://localhost:3306/cantina_db?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080
